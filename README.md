# ChwilioSearch

## To Build:
Run the following command from the project directory
./mvnw clean package

## To Run Server:
Run the following command from the project directory
java -jar target/chwilio-0.0.1-SNAPSHOT.jar
